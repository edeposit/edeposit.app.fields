from edeposit.app.fields._fields import ISBNLine, PeriodicityChoice
from edeposit.app.fields.interfaces import IISBNLine, IPeriodicity

def initialize(context):
    """Initializer called when used as a Zope 2 product."""
